# chat
Java web based chat application samlpe

Features:
 - Register user
- Login user
- Chat between two users.
For now only two users can be registered.

For database configuration, please, take a look into /WEB-INF/config.properties file.
For now the application works with mySql database.
